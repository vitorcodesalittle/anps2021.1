# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                               controllers.HomeController.index()
GET     /api/testauth                   controllers.UserController.testAuth()
POST    /api/sign-up                    controllers.UserController.signUp()
POST    /api/login                      controllers.UserController.login()


GET     /api/products                   controllers.ProductController.getProducts()
POST    /api/products                   controllers.ProductController.createProduct()

# GET     /cashflow                   controllers.TransactionController.mountCashFlow()
POST    /sales                      controllers.TransactionController.doSale()

# Map static resources from the /public folder to the /assets URL path
GET     /*file                      controllers.HomeController.assetOrDefault(file)
